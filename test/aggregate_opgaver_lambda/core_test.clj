(ns aggregate-opgaver-lambda.core-test
  (:require [clojure.test :refer :all]
            [aggregate-opgaver-lambda.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
